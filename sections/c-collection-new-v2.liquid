{% comment %}
  ╔══════════════════════════════════════════════════════════════╗
  ║  C - Collection New                                          ║
  ║  Metafields requeridos en productos:                         ║
  ║    custom.tag_one    → filtro / badge 1                      ║
  ║    custom.tag_two    → filtro / badge 2                      ║
  ║    custom.short_name → nombre corto (override del título)    ║
  ║    custom.list       → bullets (Rich Text metafield)         ║
  ╚══════════════════════════════════════════════════════════════╝
{% endcomment %}

{%- assign section_id    = section.id -%}
{%- assign default_filter = section.settings.default_filter | strip | downcase -%}

{%- comment %} ── Filtros desde custom.tag_one y custom.tag_two de metafields ── {%- endcomment %}
{%- assign seen_tags = '' -%}
{%- assign all_filter_tags = '' -%}
{%- for product in collection.products -%}
  {%- assign t1 = product.metafields.custom.tag_one.value | strip -%}
  {%- assign t2 = product.metafields.custom.tag_two.value | strip -%}
  {%- unless t1 == blank or seen_tags contains t1 -%}
    {%- assign seen_tags = seen_tags | append: t1 | append: '|||' -%}
    {%- assign all_filter_tags = all_filter_tags | append: t1 | append: '|||' -%}
  {%- endunless -%}
  {%- unless t2 == blank or seen_tags contains t2 -%}
    {%- assign seen_tags = seen_tags | append: t2 | append: '|||' -%}
    {%- assign all_filter_tags = all_filter_tags | append: t2 | append: '|||' -%}
  {%- endunless -%}
{%- endfor -%}
{%- assign filter_tags_array = all_filter_tags | split: '|||' -%}

{%- comment %} ── Si se oculta el filtro, el sidebar desaparece y esa columna se suma al grid ── {%- endcomment %}
{%- assign show_sidebar = section.settings.show_filters -%}
{%- assign columns_desktop_num = section.settings.columns_desktop | plus: 0 -%}
{%- assign grid_columns_desktop = columns_desktop_num -%}
{%- assign grid_columns_tablet  = 2 -%}
{%- unless show_sidebar -%}
  {%- assign grid_columns_desktop = columns_desktop_num | plus: 1 -%}
  {%- assign grid_columns_tablet  = 3 -%}
{%- endunless -%}

<div
  id="c-col-new-{{ section_id }}"
  class="c-col-new"
  data-section-id="{{ section_id }}"
  data-collection-handle="{{ collection.handle }}"
  data-default-filter="{{ default_filter | escape }}"
>

  {%- if show_sidebar -%}
  <!-- ══════════════════════════════════════
       SIDEBAR
  ══════════════════════════════════════ -->
  <aside class="c-col-new__sidebar" id="c-col-new-sidebar-{{ section_id }}">

    {%- if section.settings.show_search -%}
    <div class="c-col-new__search-box">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
        <circle cx="11" cy="11" r="7" stroke="currentColor" stroke-width="2"/>
        <path d="M20 20L16.65 16.65" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
      <input
        type="text"
        id="c-col-new-search-{{ section_id }}"
        class="c-col-new__search-input"
        placeholder="{{ section.settings.search_placeholder | default: 'Search' }}"
        autocomplete="off"
        aria-label="Search products"
      />
    </div>
    {%- endif -%}

    <div
      class="c-col-new__filter-section{% if section.settings.hide_filters_mobile %} c-col-new__filter-section--hide-mobile{% endif %}"
      id="c-col-new-filter-section-{{ section_id }}"
    >
      <div class="c-col-new__filter-header" role="button" aria-expanded="true" aria-controls="c-col-new-filters-{{ section_id }}">
        <span class="c-col-new__filter-label">{{ section.settings.filter_label | default: 'TYPE' }}</span>
        <svg class="c-col-new__chevron" width="14" height="14" viewBox="0 0 24 24" fill="none">
          <path d="M6 9L12 15L18 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <div class="c-col-new__filters" id="c-col-new-filters-{{ section_id }}">

        {%- comment %} "All" option {%- endcomment %}
        {%- assign all_checked = '' -%}
        {%- if default_filter == blank or default_filter == 'all' -%}
          {%- assign all_checked = 'checked' -%}
        {%- endif -%}
        <label class="c-col-new__filter-item">
          <input type="radio" name="c-col-new-type-{{ section_id }}" value="all" {{ all_checked }} class="c-col-new__radio" />
          <span class="c-col-new__radio-dot"></span>
          {{ section.settings.all_label | default: 'All' }}
        </label>

        {%- comment %} Dynamic filters from custom.tag_one / custom.tag_two metafields {%- endcomment %}
        {%- for tag in filter_tags_array -%}
          {%- assign tag_trimmed = tag | strip -%}
          {%- unless tag_trimmed == blank -%}
            {%- assign tag_lower = tag_trimmed | downcase -%}
            {%- assign tag_checked = '' -%}
            {%- if default_filter == tag_lower -%}
              {%- assign tag_checked = 'checked' -%}
            {%- endif -%}
            <label class="c-col-new__filter-item">
              <input
                type="radio"
                name="c-col-new-type-{{ section_id }}"
                value="{{ tag_trimmed | escape }}"
                {{ tag_checked }}
                class="c-col-new__radio"
              />
              <span class="c-col-new__radio-dot"></span>
              {{ tag_trimmed }}
            </label>
          {%- endunless -%}
        {%- endfor -%}

      </div>
    </div>

  </aside>
  {%- endif -%}

  <!-- ══════════════════════════════════════
       PRODUCT GRID
  ══════════════════════════════════════ -->
  <main class="c-col-new__main{% unless show_sidebar %} c-col-new__main--full{% endunless %}">
    {%- if section.settings.show_search and show_sidebar == false -%}
    <div class="c-col-new__search-box c-col-new__search-box--top">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
        <circle cx="11" cy="11" r="7" stroke="currentColor" stroke-width="2"/>
        <path d="M20 20L16.65 16.65" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
      <input
        type="text"
        id="c-col-new-search-{{ section_id }}"
        class="c-col-new__search-input"
        placeholder="{{ section.settings.search_placeholder | default: 'Search' }}"
        autocomplete="off"
        aria-label="Search products"
      />
    </div>
    {%- endif -%}
    <div class="c-col-new__grid" id="c-col-new-grid-{{ section_id }}">
      {%- assign promo_inserted = false -%}

      {%- comment %} ── Posición del promo block: la decide el admin (3ra o 4ta), por defecto 4ta ── {%- endcomment %}
      {%- assign promo_position_num = section.settings.promo_position | default: '4' | plus: 0 -%}
      {%- assign promo_target_index = promo_position_num | minus: 1 -%}

      {%- for product in collection.products -%}
        {%- assign p_img       = product.featured_image -%}
        {%- assign p_title     = product.metafields.custom.short_name.value | default: product.title -%}
        {%- assign p_price     = product.price_min | money -%}
        {%- assign p_list_raw  = product.metafields.custom.list.value | default: '' -%}
        {%- assign p_tag_one   = product.metafields.custom.tag_one.value | default: '' -%}
        {%- assign p_tag_two   = product.metafields.custom.tag_two.value | default: '' -%}
        {%- assign p_variant   = product.first_available_variant -%}
        {%- assign p_data_tags = p_tag_one | downcase | append: '|||' | append: p_tag_two | downcase -%}

        {%- comment %} Embed variants JSON for this product {%- endcomment %}
        <script type="application/json" class="c-col-new__variants-json" data-product-id="{{ product.id }}">
          [{% for v in product.variants %}{"id":{{ v.id }},"title":{{ v.title | json }},"options":{{ v.options | json }},"price":{{ v.price | json }},"price_money":{{ v.price | money | json }},"available":{{ v.available | json }}}{% unless forloop.last %},{% endunless %}{% endfor %}]
        </script>

        <div
          class="c-col-new__card"
          data-tags="{{ p_data_tags | escape }}"
          data-title="{{ product.title | downcase | escape }}"
          data-product-id="{{ product.id }}"
        >

          <!-- Image Box -->
          <div class="c-col-new__image-box">

            {%- if p_tag_one != blank or p_tag_two != blank -%}
              <div class="c-col-new__badges c-col-new__badges--{{ section.settings.badge_alignment }}">
                {%- if p_tag_one != blank -%}
                  <span class="c-col-new__badge">{{ p_tag_one }}</span>
                {%- endif -%}
                {%- if p_tag_two != blank -%}
                  <span class="c-col-new__badge">{{ p_tag_two }}</span>
                {%- endif -%}
              </div>
            {%- endif -%}

            {%- if p_img != blank -%}
              <a href="{{ product.url }}" class="c-col-new__image-link">
                <img
                  src="{{ p_img | image_url: width: 600 }}"
                  alt="{{ product.title | escape }}"
                  width="300"
                  height="300"
                  loading="lazy"
                />
              </a>
            {%- endif -%}

            {%- if section.settings.show_subscribe_save -%}
              <div class="c-col-new__subscribe-pill">
                {{ section.settings.subscribe_text | default: 'Subscribe & Save up to 15% OFF' }}
              </div>
            {%- endif -%}

          </div>

          <!-- Info -->
          <div class="c-col-new__info">

            <a href="{{ product.url }}" class="c-col-new__title-link">
              <h3 class="c-col-new__title">{{ p_title }}</h3>
            </a>

            {%- if p_list_raw != blank -%}
              <div class="c-col-new__list">
                {{ product.metafields.custom.list | metafield_tag }}
              </div>
            {%- endif -%}

            {%- comment %} ── Variant selector panel ── {%- endcomment %}
            {%- if product.has_only_default_variant == false -%}
              <div
                class="c-col-new__variant-panel"
                data-product-id="{{ product.id }}"
              >
                {%- for option in product.options_with_values -%}
                  {%- assign opt_index = forloop.index -%}
                  <div class="c-col-new__opt-group">
                    <button
                      type="button"
                      class="c-col-new__opt-header"
                      aria-expanded="{{ section.settings.variants_default_open | default: false }}"
                    >
                      <span class="c-col-new__opt-label">{{ section.settings.choose_text | default: 'Choose your' }} {{ option.name }}</span>
                      <svg class="c-col-new__opt-chevron" width="14" height="14" viewBox="0 0 24 24" fill="none">
                        <path d="M6 9L12 15L18 9" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </button>
                    <div class="c-col-new__opt-body{% if section.settings.variants_default_open %} is-open{% endif %}">
                      {%- for value in option.values -%}
                        {%- assign radio_id = 'ccn-' | append: section_id | append: '-p' | append: product.id | append: '-o' | append: opt_index | append: '-' | append: forloop.index -%}
                        {%- assign value_handle = value | handleize -%}
                        {%- assign dot_color = '' -%}
                        {%- if value_handle contains 'cool-mint' or value_handle contains 'mint' -%}
                          {%- assign dot_color = '#CDFFF1' -%}
                        {%- elsif value_handle contains 'strawberry' -%}
                          {%- assign dot_color = '#FF83BD' -%}
                        {%- elsif value_handle contains 'orange' or value_handle contains 'mango' -%}
                          {%- assign dot_color = '#FFC183' -%}
                        {%- elsif value_handle contains 'watermelon' -%}
                          {%- assign dot_color = '#FF8385' -%}
                        {%- endif -%}

                        <label class="c-col-new__opt-item" for="{{ radio_id }}">
                          <input
                            type="radio"
                            id="{{ radio_id }}"
                            class="c-col-new__opt-radio"
                            name="ccn-{{ section_id }}-p{{ product.id }}-o{{ opt_index }}"
                            value="{{ value | escape }}"
                            data-position="{{ opt_index }}"
                            data-product-id="{{ product.id }}"
                            {% if forloop.first %}checked{% endif %}
                          />
                          {%- if dot_color != '' -%}
                            <span class="c-col-new__opt-dot" style="background:{{ dot_color }};"></span>
                          {%- else -%}
                            <span class="c-col-new__opt-dot c-col-new__opt-dot--empty"></span>
                          {%- endif -%}
                          <span class="c-col-new__opt-text">{{ value }}</span>
                        </label>
                      {%- endfor -%}
                    </div>
                  </div>
                {%- endfor -%}
              </div>
            {%- endif -%}

            <div class="c-col-new__price" data-product-id="{{ product.id }}">{{ p_price }}</div>

            <div class="c-col-new__actions">
              {%- if p_variant != blank -%}
                <button
                  class="c-col-new__add-btn"
                  type="button"
                  data-product-id="{{ product.id }}"
                  data-variant-id="{{ p_variant.id }}"
                  data-product-url="{{ product.url }}"
                  data-add-text="{{ section.settings.add_to_cart_text | default: 'ADD TO CART' | escape }}"
                >
                  {{ section.settings.add_to_cart_text | default: 'ADD TO CART' }}
                </button>
              {%- endif -%}

              <a href="{{ product.url }}" class="c-col-new__learn-more">
                {{ section.settings.learn_more_text | default: 'LEARN MORE' }}
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                  <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            </div>

          </div>
        </div>

        {%- if section.settings.show_promo_block and promo_inserted == false and forloop.index == promo_target_index -%}
          <aside class="c-col-new__promo" aria-label="{{ section.settings.promo_title | escape }}">
            <div class="c-col-new__promo-card">
              {%- if section.settings.promo_image_desktop != blank or section.settings.promo_image_mobile != blank -%}
                <div class="c-col-new__promo-media">
                  <picture>
                    {%- if section.settings.promo_image_mobile != blank -%}
                      <source media="(max-width: 767px)" srcset="{{ section.settings.promo_image_mobile | image_url: width: 900 }}">
                    {%- endif -%}
                    <img
                      src="{{ section.settings.promo_image_desktop | default: section.settings.promo_image_mobile | image_url: width: 1200 }}"
                      alt="{{ section.settings.promo_image_alt | escape }}"
                      loading="lazy"
                      width="900"
                      height="900"
                      class="c-col-new__promo-image"
                    >
                  </picture>
                </div>
              {%- endif -%}

              <div class="c-col-new__promo-overlay">
                <div class="c-col-new__promo-top">
                  {%- if section.settings.promo_title != blank -%}
                    <h3 class="c-col-new__promo-title">{{ section.settings.promo_title }}</h3>
                  {%- endif -%}
                </div>

                <div class="c-col-new__promo-panel">
                  {%- if section.settings.promo_text != blank -%}
                    <div class="c-col-new__promo-text">{{ section.settings.promo_text }}</div>
                  {%- endif -%}

                  {%- if section.settings.promo_button_text != blank -%}
                    {%- if section.settings.promo_button_url != blank -%}
                      <a class="c-col-new__promo-button" href="{{ section.settings.promo_button_url }}">{{ section.settings.promo_button_text }}</a>
                    {%- else -%}
                      <span class="c-col-new__promo-button c-col-new__promo-button--static">{{ section.settings.promo_button_text }}</span>
                    {%- endif -%}
                  {%- endif -%}
                </div>
              </div>
            </div>
          </aside>
          {%- assign promo_inserted = true -%}
        {%- endif -%}
      {%- else -%}
        <p class="c-col-new__empty">{{ section.settings.empty_text | default: 'No products in this collection.' }}</p>
      {%- endfor -%}

      {%- if section.settings.show_promo_block and promo_inserted == false -%}
        <aside class="c-col-new__promo" aria-label="{{ section.settings.promo_title | escape }}">
          <div class="c-col-new__promo-card">
            {%- if section.settings.promo_image_desktop != blank or section.settings.promo_image_mobile != blank -%}
              <div class="c-col-new__promo-media">
                <picture>
                  {%- if section.settings.promo_image_mobile != blank -%}
                    <source media="(max-width: 767px)" srcset="{{ section.settings.promo_image_mobile | image_url: width: 900 }}">
                  {%- endif -%}
                  <img
                    src="{{ section.settings.promo_image_desktop | default: section.settings.promo_image_mobile | image_url: width: 1200 }}"
                    alt="{{ section.settings.promo_image_alt | escape }}"
                    loading="lazy"
                    width="900"
                    height="900"
                    class="c-col-new__promo-image"
                  >
                </picture>
              </div>
            {%- endif -%}

            <div class="c-col-new__promo-overlay">
              <div class="c-col-new__promo-top">
                {%- if section.settings.promo_title != blank -%}
                  <h3 class="c-col-new__promo-title">{{ section.settings.promo_title }}</h3>
                {%- endif -%}
              </div>

              <div class="c-col-new__promo-panel">
                {%- if section.settings.promo_text != blank -%}
                  <div class="c-col-new__promo-text">{{ section.settings.promo_text }}</div>
                {%- endif -%}

                {%- if section.settings.promo_button_text != blank -%}
                  {%- if section.settings.promo_button_url != blank -%}
                    <a class="c-col-new__promo-button" href="{{ section.settings.promo_button_url }}">{{ section.settings.promo_button_text }}</a>
                  {%- else -%}
                    <span class="c-col-new__promo-button c-col-new__promo-button--static">{{ section.settings.promo_button_text }}</span>
                  {%- endif -%}
                {%- endif -%}
              </div>
            </div>
          </div>
        </aside>
      {%- endif -%}

    </div>

    <p class="c-col-new__no-results" id="c-col-new-no-results-{{ section_id }}" style="display:none;">
      {{ section.settings.no_results_text | default: 'No products found.' }}
    </p>
  </main>

</div>


<!-- ════════════════════════════════════════════════════════════
     STYLES
════════════════════════════════════════════════════════════ -->
<style>
#c-col-new-{{ section_id }} {
  display: flex;
  gap: 40px;
  max-width: 1440px;
  margin: 0 auto;
  padding-top: {{ section.settings.padding_top_desktop }}px;
  padding-bottom: {{ section.settings.padding_bottom_desktop }}px;
  padding-left: 40px;
  padding-right: 40px;
  background-color: {{ section.settings.bg_color }};
  font-family: 'Raleway', sans-serif;
  align-items: flex-start;
  box-sizing: border-box;
}

/* ════ SIDEBAR ════ */
#c-col-new-{{ section_id }} .c-col-new__sidebar {
  width: {{ section.settings.sidebar_width }}px;
  flex-shrink: 0;
  position: sticky;
  top: 100px;
}

/* Search box */
#c-col-new-{{ section_id }} .c-col-new__search-box {
  display: flex;
  align-items: center;
  gap: 8px;
  border: 1.5px solid #F3F3F3B2;
  border-radius: 8px;
  padding: 8px 12px;
  margin-bottom: 20px;
  background: #F3F3F3B2;
  transition: border-color 0.2s ease;
}
#c-col-new-{{ section_id }} .c-col-new__search-box:focus-within {
  border-color: {{ section.settings.heading_color }};
}
#c-col-new-{{ section_id }} .c-col-new__search-box svg {
  color: #aaa;
  flex-shrink: 0;
}
#c-col-new-{{ section_id }} .c-col-new__search-input {
  border: none;
  outline: none;
  font-family: 'Raleway', sans-serif;
  font-size: 13px;
  color: #1F2023;
  width: 100%;
  background: transparent;
}
#c-col-new-{{ section_id }} .c-col-new__search-input::placeholder { color: #bbb; }

/* Filter section */
#c-col-new-{{ section_id }} .c-col-new__filter-section {
  border: 1px solid #002C41;
  border-radius: 10px;
  padding: 16px 16px 20px;
  background: #fff;
}

#c-col-new-{{ section_id }} .c-col-new__filter-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
  margin-bottom: 16px;
  user-select: none;
}
#c-col-new-{{ section_id }} .c-col-new__filter-label {
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 0.08em;
  color: #000;
}
#c-col-new-{{ section_id }} .c-col-new__chevron {
  color: #666;
  transition: transform 0.25s ease;
}
#c-col-new-{{ section_id }} .c-col-new__filter-section.is-collapsed .c-col-new__chevron {
  transform: rotate(-90deg);
}
#c-col-new-{{ section_id }} .c-col-new__filter-section.is-collapsed .c-col-new__filters {
  display: none;
}

/* Filter items */
#c-col-new-{{ section_id }} .c-col-new__filter-item {
  display: flex;
  align-items: center;
  gap: 10px;
  font-family: 'Raleway', sans-serif;
  font-size: 14px;
  color: #000;
  margin-bottom: 14px;
  cursor: pointer;
  font-weight: 700;
  user-select: none;
  transition: color 0.2s;
  line-height: 1.3;
}
#c-col-new-{{ section_id }} .c-col-new__filter-item:last-child { margin-bottom: 0; }
#c-col-new-{{ section_id }} .c-col-new__filter-item:hover { color: {{ section.settings.heading_color }}; }

#c-col-new-{{ section_id }} .c-col-new__radio { display: none; }

#c-col-new-{{ section_id }} .c-col-new__radio-dot {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  border: 2px solid #ccc;
  flex-shrink: 0;
  transition: border-color 0.2s ease;
  position: relative;
}
#c-col-new-{{ section_id }} .c-col-new__radio-dot::after {
  content: '';
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: {{ section.settings.heading_color }};
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%) scale(0);
  transition: transform 0.2s ease;
}
#c-col-new-{{ section_id }} .c-col-new__radio:checked + .c-col-new__radio-dot {
  border-color: {{ section.settings.heading_color }};
}
#c-col-new-{{ section_id }} .c-col-new__radio:checked + .c-col-new__radio-dot::after {
  transform: translate(-50%, -50%) scale(1);
}

/* ════ MAIN & GRID ════ */
#c-col-new-{{ section_id }} .c-col-new__main {
  flex: 1;
  min-width: 0;
}

#c-col-new-{{ section_id }} .c-col-new__grid {
  display: grid;
  grid-template-columns: repeat({{ grid_columns_desktop }}, 1fr);
  gap: {{ section.settings.grid_gap }}px;
}

#c-col-new-{{ section_id }} .c-col-new__main--full .c-col-new__search-box--top {
  max-width: 360px;
  margin-bottom: 24px;
}

#c-col-new-{{ section_id }} .c-col-new__promo {
  display: flex;
  min-width: 0;
}

#c-col-new-{{ section_id }} .c-col-new__promo-card {
  position: relative;
  overflow: hidden;
  border-radius: {{ section.settings.promo_radius }}px;
  background: {{ section.settings.promo_bg_color }};
  min-height: {{ section.settings.promo_height_desktop }}px;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  width: 100%;
}

#c-col-new-{{ section_id }} .c-col-new__promo-media {
  position: absolute;
  inset: 0;
}

#c-col-new-{{ section_id }} .c-col-new__promo-media picture,
#c-col-new-{{ section_id }} .c-col-new__promo-image {
  display: block;
  width: 100%;
  height: 100%;
}

#c-col-new-{{ section_id }} .c-col-new__promo-image {
  object-fit: cover;
  object-position: center center;
}

#c-col-new-{{ section_id }} .c-col-new__promo-overlay {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  min-height: {{ section.settings.promo_height_desktop }}px;
  padding: 22px 0 0;
}

#c-col-new-{{ section_id }} .c-col-new__promo-top {
  width: 100%;
  padding: 0 20px;
  box-sizing: border-box;
}

#c-col-new-{{ section_id }} .c-col-new__promo-title {
  margin: 0;
  width: 100%;
  max-width: 240px;
  box-sizing: border-box;
  font-family: {{ section.settings.promo_heading_font_family }};
  font-size: {{ section.settings.promo_title_size_desktop }}px;
  font-weight: {{ section.settings.promo_title_weight }};
  line-height: {{ section.settings.promo_title_line_height_desktop }}%;
  letter-spacing: -0.02em;
  color: {{ section.settings.promo_title_color }};
}

#c-col-new-{{ section_id }} .c-col-new__promo-panel {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  width: 100%;
  margin: 0;
  min-height: 152px;
  padding: 14px 16px 16px;
  background: rgba(255, 255, 255, 0.42);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  text-align: left;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

#c-col-new-{{ section_id }} .c-col-new__promo-text {
  margin-top: 10px;
  font-family: 'Raleway', sans-serif;
  font-size: {{ section.settings.promo_text_size_desktop }}px;
  line-height: {{ section.settings.promo_text_line_height_desktop }}%;
  color: {{ section.settings.promo_text_color }};
}

#c-col-new-{{ section_id }} .c-col-new__promo-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  margin-top: 12px;
  min-height: 50px;
  padding: 0 20px;
  border-radius: 999px;
  background: {{ section.settings.promo_button_bg_color }};
  color: {{ section.settings.promo_button_text_color }};
  font-family: 'Raleway', sans-serif;
  font-size: {{ section.settings.promo_button_font_size_desktop }}px;
  font-weight: {{ section.settings.promo_button_font_weight }};
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.02em;
  box-sizing: border-box;
}

#c-col-new-{{ section_id }} .c-col-new__promo-button--static {
  pointer-events: none;
}

#c-col-new-{{ section_id }} .c-col-new__empty,
#c-col-new-{{ section_id }} .c-col-new__no-results {
  font-family: 'Raleway', sans-serif;
  font-size: 15px;
  color: #999;
  text-align: center;
  padding: 60px 0;
  grid-column: 1 / -1;
}

/* ════ CARD ════ */
#c-col-new-{{ section_id }} .c-col-new__card {
  display: flex;
  flex-direction: column;
  background: transparent;
  transition: transform 0.3s ease;
  height: 100%;
}
#c-col-new-{{ section_id }} .c-col-new__card:hover {
  transform: translateY(-4px);
}
#c-col-new-{{ section_id }} .c-col-new__card[data-hidden="true"] {
  display: none;
}

/* Image Box */
#c-col-new-{{ section_id }} .c-col-new__image-box {
  position: relative;
  background: #F7F7F7;
  border: 1.5px solid {{ section.settings.heading_color }};
  border-radius: 12px;
  padding: 40px 20px;
  margin-bottom: 12px;
  margin-top: 18px;
  display: flex;
  justify-content: center;
  align-items: center;
  height: {{ section.settings.image_height_desktop }}px;
}
#c-col-new-{{ section_id }} .c-col-new__image-link {
  display: block;
  width: 100%;
  height: 100%;
}
#c-col-new-{{ section_id }} .c-col-new__image-link img {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

/* Badges */
#c-col-new-{{ section_id }} .c-col-new__badges {
  position: absolute;
  top: 0;
  display: flex;
  gap: 8px;
  white-space: nowrap;
  z-index: 2;
}
#c-col-new-{{ section_id }} .c-col-new__badges--left   { left: 20px; transform: translateY(-50%); }
#c-col-new-{{ section_id }} .c-col-new__badges--center { left: 50%; transform: translate(-50%, -50%); }
#c-col-new-{{ section_id }} .c-col-new__badges--right  { right: 20px; left: auto; transform: translateY(-50%); }

#c-col-new-{{ section_id }} .c-col-new__badge {
  background: #fff;
  border: 1.5px solid {{ section.settings.heading_color }};
  color: {{ section.settings.heading_color }};
  font-family: 'Raleway', sans-serif;
  font-size: 11px;
  font-weight: 700;
  padding: 6px 14px;
  border-radius: 20px;
  line-height: 1;
}

/* Subscribe pill */
#c-col-new-{{ section_id }} .c-col-new__subscribe-pill {
  position: absolute;
  bottom: 29px;
  left: 50%;
  transform: translate(-50%, 50%);
  background: #fff;
  color: #1F2023;
  font-family: 'Raleway', sans-serif;
  font-size: 10px;
  font-weight: 800;
  font-style: italic;
  padding: 6px 24px;
  border-radius: 40px;
  text-align: center;
  white-space: nowrap;
  box-shadow: 0 4px 15px rgba(0,0,0,.08);
  width: max-content;
  max-width: 90%;
  z-index: 2;
}

/* Info */
#c-col-new-{{ section_id }} .c-col-new__info {
  display: flex;
  flex-direction: column;
  flex: 1;
  gap: 12px;
  padding: 12px;
}
#c-col-new-{{ section_id }} .c-col-new__title-link { text-decoration: none; color: inherit; }
#c-col-new-{{ section_id }} .c-col-new__title {
  font-family: 'Raleway', sans-serif;
  font-size: 16px;
  font-weight: 700;
  color: {{ section.settings.heading_color }};
  margin: 0;
  line-height: 1.3;
}
#c-col-new-{{ section_id }} .c-col-new__title-link:hover .c-col-new__title { text-decoration: underline; }

#c-col-new-{{ section_id }} .c-col-new__list {
  font-family: 'Raleway', sans-serif;
  font-size: 13px;
  font-weight: 400;
  color: {{ section.settings.text_color }};
  line-height: 1.5;
}
#c-col-new-{{ section_id }} .c-col-new__list ul,
#c-col-new-{{ section_id }} .c-col-new__list ol {
  list-style: none;
  margin: 0;
  padding: 0;
}
#c-col-new-{{ section_id }} .c-col-new__list ul li,
#c-col-new-{{ section_id }} .c-col-new__list ol li {
  padding: 2px 0 2px 14px;
  position: relative;
}
#c-col-new-{{ section_id }} .c-col-new__list ul li::before,
#c-col-new-{{ section_id }} .c-col-new__list ol li::before {
  content: '\2022';
  position: absolute;
  left: 0;
  color: {{ section.settings.text_color }};
}
#c-col-new-{{ section_id }} .c-col-new__list p,
#c-col-new-{{ section_id }} .c-col-new__list h1,
#c-col-new-{{ section_id }} .c-col-new__list h2,
#c-col-new-{{ section_id }} .c-col-new__list h3 {
  margin: 0;
  font-size: 13px;
  font-weight: 400;
  color: {{ section.settings.text_color }};
}

/* ════ VARIANT PANEL ════ */
#c-col-new-{{ section_id }} .c-col-new__variant-panel {
  border-top: 1px solid #e5e5e5;
  margin-top: 4px;
  margin-bottom: 4px;
}

#c-col-new-{{ section_id }} .c-col-new__opt-group {
  border-bottom: 1px solid #e5e5e5;
}

#c-col-new-{{ section_id }} .c-col-new__opt-header {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  background: none;
  border: none;
  cursor: pointer;
  font-family: 'Raleway', sans-serif;
  gap: 8px;
}

#c-col-new-{{ section_id }} .c-col-new__opt-label {
  font-size: 13px;
  font-weight: 700;
  color: #1F2023;
  text-align: left;
}

#c-col-new-{{ section_id }} .c-col-new__opt-chevron {
  color: #666;
  flex-shrink: 0;
  transition: transform 0.2s ease;
}

#c-col-new-{{ section_id }} .c-col-new__opt-header[aria-expanded="true"] .c-col-new__opt-chevron {
  transform: rotate(180deg);
}

#c-col-new-{{ section_id }} .c-col-new__opt-body {
  display: none;
  flex-direction: column;
  gap: 10px;
  padding-bottom: 12px;
}

#c-col-new-{{ section_id }} .c-col-new__opt-body.is-open {
  display: flex;
}

#c-col-new-{{ section_id }} .c-col-new__opt-item {
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  font-family: 'Raleway', sans-serif;
  font-size: 13px;
  font-weight: 600;
  color: #1F2023;
  user-select: none;
  line-height: 1.3;
}

#c-col-new-{{ section_id }} .c-col-new__opt-radio {
  display: none;
}

/* Dot base */
#c-col-new-{{ section_id }} .c-col-new__opt-dot {
  width: 20px;
  height: 20px;
  border-radius: 50%;
  border: 2px solid #ccc;
  flex-shrink: 0;
  position: relative;
  transition: border-color 0.2s, outline 0.2s;
  box-sizing: border-box;
}

/* Dot vacío (opciones sin color: packs, sizes, etc.) */
#c-col-new-{{ section_id }} .c-col-new__opt-dot--empty {
  background: #f0f0f0 !important;
}

/* Inner dot para opciones sin color */
#c-col-new-{{ section_id }} .c-col-new__opt-dot--empty::after {
  content: '';
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: {{ section.settings.heading_color }};
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%) scale(0);
  transition: transform 0.15s ease;
}

#c-col-new-{{ section_id }} .c-col-new__opt-radio:checked + .c-col-new__opt-dot--empty {
  border-color: {{ section.settings.heading_color }};
}
#c-col-new-{{ section_id }} .c-col-new__opt-radio:checked + .c-col-new__opt-dot--empty::after {
  transform: translate(-50%, -50%) scale(1);
}

/* Dot con color (flavor): borde + outline al seleccionar */
#c-col-new-{{ section_id }} .c-col-new__opt-radio:checked + .c-col-new__opt-dot:not(.c-col-new__opt-dot--empty) {
  border-color: {{ section.settings.heading_color }};
  outline: 2px solid {{ section.settings.heading_color }};
  outline-offset: 2px;
}

/* Price */
#c-col-new-{{ section_id }} .c-col-new__price {
  font-family: 'Raleway', sans-serif;
  font-size: 20px;
  font-weight: 700;
  color: #1F2023;
  margin-top: auto;
}

/* Actions */
#c-col-new-{{ section_id }} .c-col-new__actions {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  width: 100%;
}

#c-col-new-{{ section_id }} .c-col-new__add-btn {
  width: 100%;
  padding: 14px;
  background: {{ section.settings.btn_bg_color }};
  color: {{ section.settings.btn_text_color }};
  border: none;
  border-radius: 40px;
  font-family: 'Raleway', sans-serif;
  font-size: 14px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  cursor: pointer;
  text-align: center;
  transition: background 0.3s ease, transform 0.3s ease;
}
#c-col-new-{{ section_id }} .c-col-new__add-btn:hover {
  background: {{ section.settings.btn_bg_hover }};
  transform: translateY(-2px);
}
#c-col-new-{{ section_id }} .c-col-new__add-btn:disabled {
  opacity: 0.7;
  cursor: not-allowed;
  transform: none;
}

#c-col-new-{{ section_id }} .c-col-new__learn-more {
  font-family: 'Raleway', sans-serif;
  font-size: 14px;
  font-weight: 800;
  color: {{ section.settings.link_color }};
  text-decoration: none;
  display: none;
  align-items: center;
  justify-content: center;
  gap: 6px;
  transition: opacity 0.2s ease;
}
#c-col-new-{{ section_id }} .c-col-new__learn-more:hover { opacity: 0.7; }

/* ════ RESPONSIVE ════ */
@media (max-width: 1100px) {
  #c-col-new-{{ section_id }} .c-col-new__grid {
    grid-template-columns: repeat({{ grid_columns_tablet }}, 1fr);
  }
}

@media (max-width: 768px) {
  #c-col-new-{{ section_id }} {
    display: block;
    flex-direction: column;
    padding-top: {{ section.settings.padding_top_mobile }}px;
    padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
    padding-left: 20px;
    padding-right: 20px;
    gap: 20px;
  }

  #c-col-new-{{ section_id }} .c-col-new__sidebar {
    width: 100%;
    position: static;
    padding-right: 20px;
  }

  #c-col-new-{{ section_id }} .c-col-new__main {
    margin-top: 20px;
    margin-left: 30px;
    margin-right: 40px;
  }

  #c-col-new-{{ section_id }} .c-col-new__search-box {
    margin-bottom: 16px;
  }

  #c-col-new-{{ section_id }} .c-col-new__filter-header {
    margin-bottom: 12px;
  }
  #c-col-new-{{ section_id }} .c-col-new__filters {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  #c-col-new-{{ section_id }} .c-col-new__filter-item {
    margin-bottom: 0;
    padding: 7px 16px;
    border: 1.5px solid {{ section.settings.heading_color }};
    border-radius: 40px;
    font-size: 13px;
    transition: background 0.2s ease, color 0.2s ease;
    gap: 0;
  }
  #c-col-new-{{ section_id }} .c-col-new__filter-item.is-active-pill {
    background: {{ section.settings.heading_color }};
    color: #fff;
  }
  #c-col-new-{{ section_id }} .c-col-new__radio-dot { display: none; }

  /* Oculta SOLO el bloque de filtros en mobile si el checkbox está activo
     (el buscador, si está encendido, se sigue mostrando) */
  #c-col-new-{{ section_id }} .c-col-new__filter-section--hide-mobile {
    display: none;
  }

  #c-col-new-{{ section_id }} .c-col-new__grid {
    display: grid;
    grid-template-columns: repeat({{ section.settings.columns_mobile }}, 1fr);
    gap: 16px;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo {
    order: -1;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-card {
    min-height: {{ section.settings.promo_height_mobile }}px;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-overlay {
    min-height: {{ section.settings.promo_height_mobile }}px;
    padding: 18px 0 0;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-top {
    padding: 0 16px;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-title {
    font-size: {{ section.settings.promo_title_size_mobile }}px;
    line-height: {{ section.settings.promo_title_line_height_mobile }}%;
    max-width: 180px;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-panel {
    min-height: 132px;
    padding: 12px 14px 14px;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-text {
    font-size: {{ section.settings.promo_text_size_mobile }}px;
    line-height: {{ section.settings.promo_text_line_height_mobile }}%;
  }

  #c-col-new-{{ section_id }} .c-col-new__promo-button {
    min-height: 48px;
    font-size: {{ section.settings.promo_button_font_size_mobile }}px;
  }

  #c-col-new-{{ section_id }} .c-col-new__image-box {
    height: {{ section.settings.image_height_mobile }}px;
  }
}
</style>


<!-- ════════════════════════════════════════════════════════════
     JAVASCRIPT
════════════════════════════════════════════════════════════ -->
<script>
(function () {
  var sId     = {{ section_id | json }};
  var wrapper = document.getElementById('c-col-new-' + sId);
  if (!wrapper) return;

  var grid        = wrapper.querySelector('.c-col-new__grid');
  var noResults   = wrapper.querySelector('.c-col-new__no-results');
  var searchInput = wrapper.querySelector('.c-col-new__search-input');
  var radios      = wrapper.querySelectorAll('.c-col-new__radio');
  var filterHdr   = wrapper.querySelector('.c-col-new__filter-header');
  var filterSec   = wrapper.querySelector('.c-col-new__filter-section');

  /* ── Default filter ── */
  var defaultFilter = (wrapper.dataset.defaultFilter || 'all').trim().toLowerCase();
  var activeFilter  = defaultFilter;
  var searchQuery   = '';

  radios.forEach(function(radio) {
    var val = radio.value.trim().toLowerCase();
    if (val === activeFilter) radio.checked = true;
    else if (activeFilter !== 'all' && val === 'all') radio.checked = false;
  });

  /* ════════════════════════════════════════
     VARIANTS DATA
  ════════════════════════════════════════ */
  var variantsByProduct = {};
  document.querySelectorAll('.c-col-new__variants-json').forEach(function(el) {
    var pid = el.dataset.productId;
    try { variantsByProduct[pid] = JSON.parse(el.textContent.trim()); } catch(e) {
      console.warn('[C-Collection-New] Could not parse variants for product', pid, e);
    }
  });

  function findVariant(pid, selectedOptions) {
    var variants = variantsByProduct[pid] || [];
    return variants.find(function(v) {
      return v.options.every(function(opt, i) {
        var pos = i + 1;
        return !selectedOptions[pos] || opt === selectedOptions[pos];
      });
    }) || null;
  }

  function getOptionsForCard(card) {
    var map = {};
    card.querySelectorAll('.c-col-new__opt-radio:checked').forEach(function(r) {
      map[parseInt(r.dataset.position, 10)] = r.value;
    });
    return map;
  }

  function updateCardPrice(card) {
    var pid = card.dataset.productId;
    if (!pid) return;
    var priceEl = card.querySelector('.c-col-new__price[data-product-id]');
    if (!priceEl) return;
    var variant = findVariant(pid, getOptionsForCard(card));
    if (variant) priceEl.textContent = variant.price_money;
  }

  function updateCardBtn(card) {
    var pid = card.dataset.productId;
    if (!pid) return;
    var btn = card.querySelector('.c-col-new__add-btn');
    if (!btn) return;
    var variant = findVariant(pid, getOptionsForCard(card));
    if (variant) {
      btn.dataset.variantId = variant.id;
      btn.disabled = !variant.available;
      if (!variant.available) {
        btn.textContent = 'SOLD OUT';
      } else {
        btn.textContent = btn.dataset.addText || 'ADD TO CART';
      }
    }
  }

  /* ── Variant panel accordions ── */
  grid.querySelectorAll('.c-col-new__opt-group').forEach(function(group) {
    var hdr  = group.querySelector('.c-col-new__opt-header');
    var body = group.querySelector('.c-col-new__opt-body');
    if (!hdr || !body) return;
    hdr.addEventListener('click', function() {
      var open = body.classList.toggle('is-open');
      hdr.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
  });

  /* ── Variant radio change ── */
  grid.querySelectorAll('.c-col-new__opt-radio').forEach(function(radio) {
    radio.addEventListener('change', function() {
      var card = this.closest('.c-col-new__card');
      if (!card) return;
      updateCardPrice(card);
      updateCardBtn(card);
    });
  });

  /* ── Init all cards ── */
  grid.querySelectorAll('.c-col-new__card').forEach(function(card) {
    updateCardPrice(card);
    updateCardBtn(card);
  });

  /* ════════════════════════════════════════
     CART — refreshCartUI + addItemsToCart
     (mismo patrón que c_product_page)
  ════════════════════════════════════════ */
  function refreshCartUI(response) {
    /* 1. Render sections devueltas por Shopify */
    if (response.sections) {
      Object.entries(response.sections).forEach(function(entry) {
        var id   = entry[0];
        var html = entry[1];
        var targets = document.querySelectorAll(
          '[id="shopify-section-' + id + '"], #shopify-section-' + id +
          ', [data-section-id="' + id + '"], #' + id +
          ', cart-drawer#' + id + ', cart-drawer[section="' + id + '"]'
        );
        targets.forEach(function(el) {
          var tmp   = document.createElement('div');
          tmp.innerHTML = html;
          var inner = tmp.querySelector('#shopify-section-' + id) ||
                      tmp.querySelector('#' + id) ||
                      tmp.firstElementChild;
          el.innerHTML = inner ? inner.innerHTML : html;
        });
      });
    }

    /* 2. Actualizar contador del carrito */
    if (typeof response.item_count !== 'undefined') {
      document.querySelectorAll('[data-cart-count], .cart-count, #cart-count, .header__cart-count, [aria-label*="cart"] .count').forEach(function(el) {
        el.textContent = response.item_count;
      });
      document.querySelectorAll('#cart-icon-bubble').forEach(function(el) {
        el.innerHTML = response.item_count > 0
          ? '<span class="visually-hidden">Cart</span><span class="cart-count-bubble"><span aria-hidden="true">' + response.item_count + '</span><span class="visually-hidden">' + response.item_count + ' item' + (response.item_count !== 1 ? 's' : '') + '</span></span>'
          : '';
      });
    }

    /* 3. Disparar eventos estándar */
    document.dispatchEvent(new CustomEvent('cart:updated',             { detail: response }));
    document.dispatchEvent(new CustomEvent('cart:refresh',             { detail: response }));
    document.dispatchEvent(new CustomEvent('theme:cart:added',         { detail: response }));
    document.dispatchEvent(new CustomEvent('eurus:cart:items-changed', { detail: response }));

    /* 4. Abrir el drawer — multi-fallback */
    (function openDrawer() {
      /* Alpine / Eurus / Symmetry */
      if (typeof window.Alpine !== 'undefined') {
        var miniCart = Alpine.store('xMiniCart');
        var popup    = Alpine.store('xPopup');
        if (miniCart && typeof miniCart.openCart === 'function') {
          if (popup && typeof popup.close === 'function') popup.close();
          miniCart.openCart();
          document.dispatchEvent(new CustomEvent('eurus:cart:redirect'));
          return;
        }
      }
      /* Shopify theme object */
      if (window.Shopify && window.Shopify.theme && window.Shopify.theme.cartDrawer &&
          typeof window.Shopify.theme.cartDrawer.open === 'function') {
        window.Shopify.theme.cartDrawer.open();
        return;
      }
      /* cart-drawer custom element (Dawn y similares) */
      var cartDrawer = document.querySelector('cart-drawer');
      if (cartDrawer && typeof cartDrawer.open === 'function') {
        cartDrawer.open();
        return;
      }
      /* Final fallback: ask the theme to open via its own triggers/events */
      document.dispatchEvent(new CustomEvent('cart:open', { bubbles: true, detail: response }));
      document.dispatchEvent(new CustomEvent('cartdrawer:open', { bubbles: true, detail: response }));
      var trigger = document.querySelector(
        '[data-cart-drawer-toggle], [data-cart-toggle], [aria-controls="mini-cart"], [aria-controls="CartDrawer"], [aria-controls="cart-drawer"], #cart-icon-bubble'
      );
      if (trigger && typeof trigger.click === 'function') {
        trigger.click();
      }
    })();
  }

  async function addItemsToCart(items) {
    /* Recopilar section IDs del drawer para que Shopify devuelva HTML actualizado */
    var sectionIds = [];
    document.querySelectorAll('[id^="shopify-section-"], cart-drawer[section], [data-section-id]').forEach(function(el) {
      var id = el.getAttribute('section') || el.dataset.sectionId || el.id.replace('shopify-section-', '');
      if (id && !sectionIds.includes(id) &&
          (el.id.includes('cart') || el.id.includes('mini') || el.getAttribute('section'))) {
        sectionIds.push(id);
      }
    });
    ['mini-cart', 'cart-drawer'].forEach(function(id) {
      if (!sectionIds.includes(id)) sectionIds.push(id);
    });

    var response = await fetch((window.Shopify.routes.root || '/') + 'cart/add.js', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'X-Requested-With': 'XMLHttpRequest' },
      body: JSON.stringify({ items: items, sections: sectionIds })
    });
    var payload = await response.json();
    if (!response.ok || payload.status === 422) {
      throw new Error(payload.description || 'Add to cart failed');
    }

    /* Obtener item_count actualizado */
    try {
      var cartResp = await fetch((window.Shopify.routes.root || '/') + 'cart.js', {
        headers: { Accept: 'application/json', 'X-Requested-With': 'XMLHttpRequest' }
      });
      if (cartResp.ok) {
        var cartData = await cartResp.json();
        payload.item_count = cartData.item_count;
      }
    } catch(_) {}

    refreshCartUI(payload);
    return payload;
  }

  /* ── Add to Cart click ── */
  grid.querySelectorAll('.c-col-new__add-btn').forEach(function(btn) {
    btn.addEventListener('click', async function() {
      var variantId = this.dataset.variantId;
      var addText   = this.dataset.addText || 'ADD TO CART';
      if (!variantId) return;

      this.disabled    = true;
      this.textContent = 'ADDING...';
      var self = this;

      try {
        await addItemsToCart([{ id: Number(variantId), quantity: 1 }]);
        self.textContent = addText;
        self.disabled    = false;
      } catch(err) {
        console.error('[C-Collection-New] ATC error:', err);
        self.textContent = 'ERROR';
        setTimeout(function() {
          self.textContent = addText;
          self.disabled    = false;
        }, 1400);
      }
    });
  });

  /* ════════════════════════════════════════
     FILTER + SEARCH
  ════════════════════════════════════════ */
  function applyFilters() {
    var visible = 0;
    grid.querySelectorAll('.c-col-new__card').forEach(function(card) {
      var tags  = (card.dataset.tags  || '').toLowerCase();
      var title = (card.dataset.title || '').toLowerCase();
      var matchFilter = activeFilter === 'all' || tags.indexOf(activeFilter) !== -1;
      var matchSearch = searchQuery === '' || title.indexOf(searchQuery) !== -1 || tags.indexOf(searchQuery) !== -1;
      var show = matchFilter && matchSearch;
      card.setAttribute('data-hidden', show ? 'false' : 'true');
      if (show) visible++;
    });

    if (noResults) noResults.style.display = visible === 0 ? 'block' : 'none';

    wrapper.querySelectorAll('.c-col-new__filter-item').forEach(function(label) {
      var radio = label.querySelector('.c-col-new__radio');
      if (radio && radio.checked) label.classList.add('is-active-pill');
      else label.classList.remove('is-active-pill');
    });
  }

  radios.forEach(function(radio) {
    radio.addEventListener('change', function() {
      if (this.checked) {
        activeFilter = this.value.trim().toLowerCase();
        applyFilters();
      }
    });
  });

  if (searchInput) {
    searchInput.addEventListener('input', function() {
      searchQuery = this.value.trim().toLowerCase();
      applyFilters();
    });
  }

  /* ── Sidebar collapse ── */
  if (filterHdr && filterSec) {
    filterHdr.addEventListener('click', function() {
      var collapsed = filterSec.classList.toggle('is-collapsed');
      filterHdr.setAttribute('aria-expanded', collapsed ? 'false' : 'true');
    });
  }

  /* ── Init ── */
  applyFilters();

})();
</script>


{% schema %}
{
  "name": "C - Collection New v2",
  "class": "section-c-collection-new",
  "settings": [
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading & Accent Color",
      "default": "#002C41"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Body Text Color",
      "default": "#4A4A4A"
    },
    {
      "type": "color",
      "id": "link_color",
      "label": "Learn More Link Color",
      "default": "#002C41"
    },
    {
      "type": "color",
      "id": "btn_bg_color",
      "label": "Button Background",
      "default": "#1164E8"
    },
    {
      "type": "color",
      "id": "btn_text_color",
      "label": "Button Text Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "btn_bg_hover",
      "label": "Button Hover Background",
      "default": "#0D50B8"
    },
    {
      "type": "header",
      "content": "Sidebar & Filters"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Show Search Box",
      "default": true
    },
    {
      "type": "text",
      "id": "search_placeholder",
      "label": "Search Placeholder",
      "default": "Search"
    },
    {
      "type": "checkbox",
      "id": "show_filters",
      "label": "Show Filters",
      "default": true,
      "info": "Desactiva esto para ocultar el sidebar completo (buscador + filtros) en TODOS los tamaños de pantalla. Ese espacio pasa a formar parte del grid como una columna extra de productos."
    },
    {
      "type": "checkbox",
      "id": "hide_filters_mobile",
      "label": "Ocultar SOLO los filtros en Mobile",
      "default": false,
      "info": "Solo aplica si \"Show Filters\" está activo arriba. Oculta únicamente el bloque de filtros en pantallas chicas (el buscador, si está activo, se sigue mostrando)."
    },
    {
      "type": "text",
      "id": "filter_label",
      "label": "Filter Section Label",
      "default": "TYPE"
    },
    {
      "type": "text",
      "id": "all_label",
      "label": "\"All\" Filter Label",
      "default": "All"
    },
    {
      "type": "text",
      "id": "default_filter",
      "label": "Default Selected Filter",
      "info": "Escribe exactamente el valor del tag_one o tag_two que quieres seleccionado por defecto (ej: Fluoride-Free). Deja vacío para mostrar 'All' por defecto."
    },
    {
      "type": "range",
      "id": "sidebar_width",
      "min": 140,
      "max": 280,
      "step": 10,
      "unit": "px",
      "label": "Sidebar Width (Desktop)",
      "default": 180
    },
    {
      "type": "header",
      "content": "Cards"
    },
    {
      "type": "header",
      "content": "Promo block"
    },
    {
      "type": "checkbox",
      "id": "show_promo_block",
      "label": "Show promo block",
      "default": false
    },
    {
      "type": "select",
      "id": "promo_position",
      "label": "Posición del promo block",
      "options": [
        { "value": "3", "label": "3ra posición" },
        { "value": "4", "label": "4ta posición" }
      ],
      "default": "4",
      "info": "En qué lugar de la grilla aparece el promo block. Si hay menos productos que esa posición, se muestra al final."
    },
    {
      "type": "text",
      "id": "promo_title",
      "label": "Promo title",
      "default": "Find the best for you"
    },
    {
      "type": "textarea",
      "id": "promo_text",
      "label": "Promo text",
      "default": "Different smiles, different needs.\nSee how our formulas compare."
    },
    {
      "type": "text",
      "id": "promo_button_text",
      "label": "Promo button text",
      "default": "COMPARE ALL"
    },
    {
      "type": "url",
      "id": "promo_button_url",
      "label": "Promo button link"
    },
    {
      "type": "image_picker",
      "id": "promo_image_desktop",
      "label": "Promo image desktop"
    },
    {
      "type": "image_picker",
      "id": "promo_image_mobile",
      "label": "Promo image mobile"
    },
    {
      "type": "text",
      "id": "promo_image_alt",
      "label": "Promo image alt text",
      "default": "Find the best for you"
    },
    {
      "type": "color",
      "id": "promo_bg_color",
      "label": "Promo background",
      "default": "#EAF2F3"
    },
    {
      "type": "color",
      "id": "promo_title_color",
      "label": "Promo title color",
      "default": "#0C3550"
    },
    {
      "type": "color",
      "id": "promo_text_color",
      "label": "Promo text color",
      "default": "#0C3550"
    },
    {
      "type": "color",
      "id": "promo_button_bg_color",
      "label": "Promo button background",
      "default": "#07324B"
    },
    {
      "type": "color",
      "id": "promo_button_text_color",
      "label": "Promo button text color",
      "default": "#FFFFFF"
    },
    {
      "type": "text",
      "id": "promo_heading_font_family",
      "label": "Promo title font family",
      "default": "var(--Heading, \"IvyPresto Display\", serif)"
    },
    {
      "type": "range",
      "id": "promo_title_size_desktop",
      "label": "Promo title size desktop",
      "min": 24,
      "max": 72,
      "step": 2,
      "default": 40
    },
    {
      "type": "range",
      "id": "promo_title_size_mobile",
      "label": "Promo title size mobile",
      "min": 20,
      "max": 56,
      "step": 2,
      "default": 32
    },
    {
      "type": "range",
      "id": "promo_title_weight",
      "label": "Promo title weight",
      "min": 300,
      "max": 800,
      "step": 100,
      "default": 400
    },
    {
      "type": "range",
      "id": "promo_title_line_height_desktop",
      "label": "Promo title line height desktop %",
      "min": 80,
      "max": 140,
      "step": 1,
      "default": 92
    },
    {
      "type": "range",
      "id": "promo_title_line_height_mobile",
      "label": "Promo title line height mobile %",
      "min": 80,
      "max": 140,
      "step": 1,
      "default": 92
    },
    {
      "type": "range",
      "id": "promo_text_size_desktop",
      "label": "Promo text size desktop",
      "min": 10,
      "max": 20,
      "step": 1,
      "default": 16
    },
    {
      "type": "range",
      "id": "promo_text_size_mobile",
      "label": "Promo text size mobile",
      "min": 10,
      "max": 18,
      "step": 1,
      "default": 14
    },
    {
      "type": "range",
      "id": "promo_text_line_height_desktop",
      "label": "Promo text line height desktop %",
      "min": 100,
      "max": 180,
      "step": 1,
      "default": 120
    },
    {
      "type": "range",
      "id": "promo_text_line_height_mobile",
      "label": "Promo text line height mobile %",
      "min": 100,
      "max": 180,
      "step": 1,
      "default": 120
    },
    {
      "type": "range",
      "id": "promo_button_font_size_desktop",
      "label": "Promo button font size desktop",
      "min": 10,
      "max": 18,
      "step": 1,
      "default": 14
    },
    {
      "type": "range",
      "id": "promo_button_font_size_mobile",
      "label": "Promo button font size mobile",
      "min": 10,
      "max": 18,
      "step": 1,
      "default": 13
    },
    {
      "type": "range",
      "id": "promo_button_font_weight",
      "label": "Promo button font weight",
      "min": 400,
      "max": 900,
      "step": 100,
      "default": 700
    },
    {
      "type": "range",
      "id": "promo_height_desktop",
      "label": "Promo height desktop",
      "min": 320,
      "max": 700,
      "step": 10,
      "default": 560
    },
    {
      "type": "range",
      "id": "promo_height_mobile",
      "label": "Promo height mobile",
      "min": 240,
      "max": 560,
      "step": 10,
      "default": 360
    },
    {
      "type": "range",
      "id": "promo_radius",
      "label": "Promo radius",
      "min": 0,
      "max": 30,
      "step": 1,
      "default": 16
    },
    {
      "type": "checkbox",
      "id": "show_subscribe_save",
      "label": "Show Subscribe & Save Pill",
      "default": true
    },
    {
      "type": "text",
      "id": "subscribe_text",
      "label": "Subscribe Pill Text",
      "default": "Subscribe & Save up to 15% OFF"
    },
    {
      "type": "select",
      "id": "badge_alignment",
      "label": "Badge Alignment",
      "options": [
        { "value": "left",   "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right",  "label": "Right" }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "add_to_cart_text",
      "label": "Add to Cart Button Text",
      "default": "ADD TO CART"
    },
    {
      "type": "text",
      "id": "learn_more_text",
      "label": "Learn More Link Text",
      "default": "LEARN MORE"
    },
    {
      "type": "text",
      "id": "no_results_text",
      "label": "No Results Message",
      "default": "No products found."
    },
    {
      "type": "text",
      "id": "empty_text",
      "label": "Empty Collection Message",
      "default": "No products in this collection."
    },
    {
      "type": "header",
      "content": "Variant Selector"
    },
    {
      "type": "checkbox",
      "id": "variants_default_open",
      "label": "Mostrar variantes abiertas por defecto",
      "default": false,
      "info": "Si está activo, los grupos de opciones (flavor, pack, etc.) aparecen desplegados al cargar la página."
    },
    {
      "type": "text",
      "id": "choose_text",
      "label": "Prefijo de opciones",
      "default": "Choose your",
      "info": "Texto que aparece antes del nombre de la opción. Ej: 'Choose your Flavor'."
    },
    {
      "type": "header",
      "content": "Desktop Layout"
    },
    {
      "type": "select",
      "id": "columns_desktop",
      "label": "Columns",
      "options": [
        { "value": "2", "label": "2" },
        { "value": "3", "label": "3" },
        { "value": "4", "label": "4" }
      ],
      "default": "3"
    },
    {
      "type": "range",
      "id": "grid_gap",
      "min": 12,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "Grid Gap",
      "default": 32
    },
    {
      "type": "range",
      "id": "image_height_desktop",
      "min": 200,
      "max": 500,
      "step": 10,
      "unit": "px",
      "label": "Image Box Height",
      "default": 320
    },
    {
      "type": "range",
      "id": "padding_top_desktop",
      "min": 0,
      "max": 120,
      "step": 5,
      "unit": "px",
      "label": "Padding Top",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom_desktop",
      "min": 0,
      "max": 120,
      "step": 5,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 60
    },
    {
      "type": "header",
      "content": "Mobile Layout"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "label": "Columnas en móvil",
      "options": [
        { "value": "1", "label": "1 columna" },
        { "value": "2", "label": "2 columnas (grid)" }
      ],
      "default": "1"
    },
    {
      "type": "range",
      "id": "image_height_mobile",
      "min": 200,
      "max": 420,
      "step": 10,
      "unit": "px",
      "label": "Image Box Height",
      "default": 280
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 40
    }
  ],
  "presets": [
    {
      "name": "C - Collection New v2"
    }
  ]
}
{% endschema %}
